// Copy this file to config.js and fill in your values.
// config.js is git-ignored — never commit real credentials.
window.__API_URL__ = '';  // leave empty when served via nginx proxy; set to 'http://localhost:3000' for local dev
window.__GOOGLE_CLIENT_ID__ = 'your-client-id.apps.googleusercontent.com';
